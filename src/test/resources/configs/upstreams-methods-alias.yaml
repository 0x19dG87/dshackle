version: v1

upstreams:
  - id: local
    chain: arbitrum
    methods:
      enabled:
        - name: trace_filter
          alias: arbtrace_filter
    connection:
      ethereum:
        rpc:
          url: "http://localhost:8545"
        ws:
          url: "ws://localhost:8546"
          origin: "http://localhost"
